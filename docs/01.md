好。**v0.4 是从“能跑”进入“能被 Google 系统性发现”的版本。**

这一版我们不再堆计算器，而是把 SEO 基础设施一次搭好：

```text
SellerCalc AI
│
├── /                         首页
├── /tools                    工具中心
│
├── /profit-calculator
├── /fee-calculator
├── /roas-calculator
│
├── /blog                     Blog 首页
│   ├── /how-to-calculate-tiktok-shop-profit
│   ├── /tiktok-shop-fees-guide
│   └── /tiktok-shop-roas-guide
│
├── /calculators
│   ├── /tiktok-shop-profit-us
│   ├── /tiktok-shop-profit-uk
│   ├── /tiktok-shop-profit-canada
│   └── /tiktok-shop-profit-australia
│
├── sitemap.xml
└── robots.txt
```

不过先提醒一个重要点：

**不要批量生成 1000 个“换个国家名字、内容完全一样”的页面。**
Google 对这种低价值 programmatic SEO 很敏感。我们的程序化页面必须有真实差异，例如地区费率、币种、FAQ、示例和计算逻辑不同。

------

# 一、先升级目录

把项目整理成：

```text
src/
├── app/
│   ├── page.tsx
│   ├── layout.tsx
│   ├── robots.ts
│   ├── sitemap.ts
│   │
│   ├── tools/
│   │   └── page.tsx
│   │
│   ├── profit-calculator/
│   │   └── page.tsx
│   │
│   ├── fee-calculator/
│   │   └── page.tsx
│   │
│   ├── roas-calculator/
│   │   └── page.tsx
│   │
│   ├── blog/
│   │   ├── page.tsx
│   │   └── [slug]/
│   │       └── page.tsx
│   │
│   └── calculators/
│       └── [slug]/
│           └── page.tsx
│
├── components/
│   ├── FAQ.tsx
│   └── CalculatorCard.tsx
│
└── lib/
    ├── blog.ts
    └── regionalCalculators.ts
```

------

# 二、先把网站基础 SEO 做正确

## `src/app/layout.tsx`

把之前的 metadata 升级成：

```tsx
import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  metadataBase: new URL("https://sellercalc.ai"),

  title: {
    default: "SellerCalc AI - TikTok Seller Tools",
    template: "%s | SellerCalc AI",
  },

  description:
    "Free TikTok Shop calculators for sellers. Calculate profit, fees, ROAS, margins and break-even prices.",

  keywords: [
    "TikTok Shop Profit Calculator",
    "TikTok Shop Fee Calculator",
    "TikTok Shop ROAS Calculator",
    "TikTok Seller Tools",
  ],

  openGraph: {
    title: "SellerCalc AI - TikTok Seller Tools",
    description:
      "Free calculators for TikTok Shop sellers.",
    type: "website",
    siteName: "SellerCalc AI",
  },

  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
```

### 注意

这里的：

```text
https://sellercalc.ai
```

只是**假设域名**。

如果你最终买的是其他域名，必须换成你的真实域名。

------

# 三、Sitemap

这是 v0.4 最重要的文件之一。

新建：

```text
src/app/sitemap.ts
import type { MetadataRoute } from "next";

const baseUrl = "https://sellercalc.ai";

export default function sitemap(): MetadataRoute.Sitemap {
  const now = new Date();

  return [
    {
      url: baseUrl,
      lastModified: now,
      changeFrequency: "weekly",
      priority: 1,
    },

    {
      url: `${baseUrl}/tools`,
      lastModified: now,
      changeFrequency: "weekly",
      priority: 0.9,
    },

    {
      url: `${baseUrl}/profit-calculator`,
      lastModified: now,
      changeFrequency: "weekly",
      priority: 0.9,
    },

    {
      url: `${baseUrl}/fee-calculator`,
      lastModified: now,
      changeFrequency: "weekly",
      priority: 0.9,
    },

    {
      url: `${baseUrl}/roas-calculator`,
      lastModified: now,
      changeFrequency: "weekly",
      priority: 0.9,
    },

    {
      url: `${baseUrl}/blog`,
      lastModified: now,
      changeFrequency: "weekly",
      priority: 0.7,
    },

    {
      url: `${baseUrl}/blog/how-to-calculate-tiktok-shop-profit`,
      lastModified: now,
      changeFrequency: "monthly",
      priority: 0.7,
    },

    {
      url: `${baseUrl}/blog/tiktok-shop-fees-guide`,
      lastModified: now,
      changeFrequency: "monthly",
      priority: 0.7,
    },

    {
      url: `${baseUrl}/blog/tiktok-shop-roas-guide`,
      lastModified: now,
      changeFrequency: "monthly",
      priority: 0.7,
    },

    {
      url: `${baseUrl}/calculators/tiktok-shop-profit-us`,
      lastModified: now,
      changeFrequency: "monthly",
      priority: 0.7,
    },

    {
      url: `${baseUrl}/calculators/tiktok-shop-profit-uk`,
      lastModified: now,
      changeFrequency: "monthly",
      priority: 0.7,
    },

    {
      url: `${baseUrl}/calculators/tiktok-shop-profit-canada`,
      lastModified: now,
      changeFrequency: "monthly",
      priority: 0.7,
    },

    {
      url: `${baseUrl}/calculators/tiktok-shop-profit-australia`,
      lastModified: now,
      changeFrequency: "monthly",
      priority: 0.7,
    },
  ];
}
```

部署后：

```text
https://你的域名/sitemap.xml
```

应该能够访问。

------

# 四、robots.txt

新建：

```text
src/app/robots.ts
import type { MetadataRoute } from "next";

const baseUrl = "https://sellercalc.ai";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: "*",
      allow: "/",
    },

    sitemap: `${baseUrl}/sitemap.xml`,
  };
}
```

这样：

```text
/robots.txt
```

会自动生成。

------

# 五、建立 Blog 数据层

我们第一版不用数据库。

直接用 TypeScript。

新建：

```text
src/lib/blog.ts
export type BlogPost = {
  slug: string;
  title: string;
  description: string;
  publishedAt: string;
  content: string[];
};

export const blogPosts: BlogPost[] = [
  {
    slug: "how-to-calculate-tiktok-shop-profit",

    title: "How to Calculate TikTok Shop Profit",

    description:
      "Learn how to calculate TikTok Shop profit after product costs, platform fees, affiliate commissions and advertising expenses.",

    publishedAt: "2026-08-18",

    content: [
      "TikTok Shop revenue is not the same as profit. Sellers need to account for product costs, platform fees, shipping, affiliate commissions and advertising expenses.",

      "A simple profit formula is: Selling Price minus Product Cost, Shipping Cost, Platform Fees, Affiliate Commission and Advertising Cost.",

      "For example, if a product sells for $40 and the total costs are $28, the estimated profit is $12.",

      "Use the SellerCalc AI TikTok Shop Profit Calculator to estimate your margin before launching or scaling a product.",
    ],
  },

  {
    slug: "tiktok-shop-fees-guide",

    title: "TikTok Shop Fees Explained",

    description:
      "A practical guide to the different costs TikTok Shop sellers should consider when pricing products.",

    publishedAt: "2026-08-18",

    content: [
      "TikTok Shop sellers need to consider several costs when calculating product profitability.",

      "Depending on the market, product category and seller configuration, applicable platform charges can vary.",

      "Instead of relying on a single percentage, sellers should calculate their complete cost structure before setting a selling price.",

      "SellerCalc AI provides a simple fee calculator to help sellers estimate these costs.",
    ],
  },

  {
    slug: "tiktok-shop-roas-guide",

    title: "What Is a Good TikTok Shop ROAS?",

    description:
      "Understand ROAS and why a high ROAS does not always mean a profitable TikTok Shop product.",

    publishedAt: "2026-08-18",

    content: [
      "ROAS stands for Return on Advertising Spend.",

      "The basic formula is revenue generated by advertising divided by advertising spend.",

      "A 3x ROAS means that every $1 spent on advertising generated $3 in attributed revenue.",

      "However, ROAS alone does not determine profitability. Product cost, marketplace fees, affiliate commissions and other expenses also matter.",
    ],
  },
];
```

------

# 六、Blog 首页

新建：

```text
src/app/blog/page.tsx
import Link from "next/link";
import { blogPosts } from "@/lib/blog";

export const metadata = {
  title: "TikTok Shop Seller Guides",
  description:
    "Practical guides about TikTok Shop profit, fees, ROAS and product pricing.",
};

export default function BlogPage() {
  return (
    <main className="min-h-screen bg-gray-50">
      <section className="mx-auto max-w-5xl px-6 py-20">

        <h1 className="text-4xl font-bold">
          TikTok Shop Seller Guides
        </h1>

        <p className="mt-4 text-gray-600">
          Practical guides to help TikTok Shop sellers
          understand fees, profit margins and advertising.
        </p>

        <div className="mt-12 grid gap-6">

          {blogPosts.map((post) => (
            <article
              key={post.slug}
              className="rounded-xl bg-white p-6 shadow-sm"
            >
              <h2 className="text-2xl font-bold">
                <Link href={`/blog/${post.slug}`}>
                  {post.title}
                </Link>
              </h2>

              <p className="mt-3 text-gray-600">
                {post.description}
              </p>

              <p className="mt-4 text-sm text-gray-400">
                {post.publishedAt}
              </p>
            </article>
          ))}

        </div>

      </section>
    </main>
  );
}
```

------

# 七、动态 Blog 页面

新建：

```text
src/app/blog/[slug]/page.tsx
import { notFound } from "next/navigation";
import { blogPosts } from "@/lib/blog";

type Props = {
  params: Promise<{
    slug: string;
  }>;
};

export async function generateStaticParams() {
  return blogPosts.map((post) => ({
    slug: post.slug,
  }));
}

export async function generateMetadata({ params }: Props) {
  const { slug } = await params;

  const post = blogPosts.find(
    (item) => item.slug === slug
  );

  if (!post) {
    return {};
  }

  return {
    title: post.title,
    description: post.description,
  };
}

export default async function BlogPost({ params }: Props) {
  const { slug } = await params;

  const post = blogPosts.find(
    (item) => item.slug === slug
  );

  if (!post) {
    notFound();
  }

  return (
    <main className="min-h-screen bg-white">
      <article className="mx-auto max-w-3xl px-6 py-20">

        <p className="text-sm text-gray-500">
          {post.publishedAt}
        </p>

        <h1 className="mt-3 text-4xl font-bold">
          {post.title}
        </h1>

        <p className="mt-5 text-lg text-gray-600">
          {post.description}
        </p>

        <div className="mt-10 space-y-6">

          {post.content.map((paragraph, index) => (
            <p
              key={index}
              className="leading-8 text-gray-700"
            >
              {paragraph}
            </p>
          ))}

        </div>

      </article>
    </main>
  );
}
```

这里用 `generateStaticParams()`，所以这几个文章页面可以在构建时直接生成。

------

# 八、开始做 Programmatic SEO

这里才是重点。

我们先不要搞100个页面。

先做：

```text
US
UK
Canada
Australia
```

以后再扩。

------

# 九、建立地区计算器数据

新建：

```text
src/lib/regionalCalculators.ts
export type RegionalCalculator = {
  slug: string;
  country: string;
  currency: string;
  currencySymbol: string;
  title: string;
  description: string;
};

export const regionalCalculators: RegionalCalculator[] = [
  {
    slug: "tiktok-shop-profit-us",
    country: "United States",
    currency: "USD",
    currencySymbol: "$",
    title: "TikTok Shop Profit Calculator USA",
    description:
      "Calculate estimated TikTok Shop profit in the United States.",
  },

  {
    slug: "tiktok-shop-profit-uk",
    country: "United Kingdom",
    currency: "GBP",
    currencySymbol: "£",
    title: "TikTok Shop Profit Calculator UK",
    description:
      "Calculate estimated TikTok Shop profit in the United Kingdom.",
  },

  {
    slug: "tiktok-shop-profit-canada",
    country: "Canada",
    currency: "CAD",
    currencySymbol: "C$",
    title: "TikTok Shop Profit Calculator Canada",
    description:
      "Calculate estimated TikTok Shop profit in Canada.",
  },

  {
    slug: "tiktok-shop-profit-australia",
    country: "Australia",
    currency: "AUD",
    currencySymbol: "A$",
    title: "TikTok Shop Profit Calculator Australia",
    description:
      "Calculate estimated TikTok Shop profit in Australia.",
  },
];
```

------

# 十、动态生成地区页面

新建：

```text
src/app/calculators/[slug]/page.tsx
import { notFound } from "next/navigation";
import { regionalCalculators } from "@/lib/regionalCalculators";

type Props = {
  params: Promise<{
    slug: string;
  }>;
};

export async function generateStaticParams() {
  return regionalCalculators.map((calculator) => ({
    slug: calculator.slug,
  }));
}

export async function generateMetadata({ params }: Props) {
  const { slug } = await params;

  const calculator = regionalCalculators.find(
    (item) => item.slug === slug
  );

  if (!calculator) {
    return {};
  }

  return {
    title: calculator.title,
    description: calculator.description,
  };
}

export default async function RegionalCalculator({
  params,
}: Props) {
  const { slug } = await params;

  const calculator = regionalCalculators.find(
    (item) => item.slug === slug
  );

  if (!calculator) {
    notFound();
  }

  return (
    <main className="min-h-screen bg-gray-50">
      <section className="mx-auto max-w-4xl px-6 py-20">

        <p className="text-sm font-medium text-gray-500">
          {calculator.country}
        </p>

        <h1 className="mt-3 text-4xl font-bold">
          {calculator.title}
        </h1>

        <p className="mt-5 text-lg text-gray-600">
          {calculator.description}
        </p>

        <div className="mt-10 rounded-2xl bg-white p-8 shadow">

          <h2 className="text-2xl font-bold">
            Calculate Your TikTok Shop Profit
          </h2>

          <div className="mt-6 space-y-4">

            <input
              placeholder={`Product Price (${calculator.currency})`}
              className="input"
            />

            <input
              placeholder={`Product Cost (${calculator.currency})`}
              className="input"
            />

            <input
              placeholder="Shipping Cost"
              className="input"
            />

            <input
              placeholder="Affiliate Commission %"
              className="input"
            />

            <input
              placeholder="Advertising Cost"
              className="input"
            />

            <button className="rounded-xl bg-black px-6 py-3 text-white">
              Calculate Profit
            </button>

          </div>

        </div>

        <section className="mt-16">

          <h2 className="text-2xl font-bold">
            TikTok Shop Profit in {calculator.country}
          </h2>

          <p className="mt-4 leading-8 text-gray-700">
            TikTok Shop sellers in {calculator.country}
            should consider product costs, shipping,
            applicable platform fees, affiliate commissions
            and advertising expenses when calculating
            product profitability.
          </p>

        </section>

      </section>
    </main>
  );
}
```

------

# 十一、这里有一个问题

上面的地区页面目前只是**SEO骨架**。

它还没有真正接我们的 Calculator。

这不是问题。

下一步把：

```text
Calculator.tsx
```

改造成：

```text
<ProfitCalculator
  currency="$"
/>
```

这样：

```text
US → $
UK → £
Canada → C$
Australia → A$
```

就能复用同一套计算器。

------

# 十二、不要现在生成100个国家页面

我们第一批只做：

```text
/tiktok-shop-profit-calculator

/tiktok-shop-profit-us
/tiktok-shop-profit-uk
/tiktok-shop-profit-canada
/tiktok-shop-profit-australia
```

然后观察 Google Search Console。

如果：

```text
Impressions ↑
Clicks ↑
Average Position ↑
```

再扩展：

```text
Germany
France
Spain
Italy
Mexico
Singapore
```

------

# 十三、Google Search Console 上线

网站部署到 Vercel 后：

### 第一步

打开 Google Search Console。

添加：

```text
Domain Property
```

例如：

```text
sellercalc.ai
```

Google 会要求你验证域名。

一般推荐：

**DNS TXT 验证。**

------

### 第二步

验证成功后：

进入：

```text
Sitemaps
```

提交：

```text
sitemap.xml
```

也就是：

```text
https://你的域名/sitemap.xml
```

------

### 第三步

手动检查几个页面：

```text
/
 /tools
 /profit-calculator
 /fee-calculator
 /roas-calculator
 /blog
 /blog/how-to-calculate-tiktok-shop-profit
 /calculators/tiktok-shop-profit-us
```

然后在 Search Console 的 URL Inspection 中：

**Request Indexing**

不用每个页面天天点。

------

# 十四、v0.4 完成后的 SEO 架构

现在我们实际上已经建立了：

```text
                 SellerCalc AI
                      │
        ┌─────────────┼─────────────┐
        ↓             ↓             ↓
      Tools          Blog       Calculators
        │             │             │
        ↓             ↓             ↓
   Profit/Fee/ROAS   Guides      US / UK / CA / AU
        │
        ↓
      Google
        │
        ↓
      Organic
        │
        ↓
       User
```

这才开始像一个真正的 SEO 产品。

------

# 十五、但我建议你现在先别继续加页面

下一步最重要的不是 v0.5 再造 20 个工具。

而是把**第一个工具真正做专业**。

目前我们的 Profit Calculator 有一个严重问题：

> 我们之前直接写死了 `6%` TikTok fee。

**这个不能直接上线。**

平台费率会根据市场、商品类别、卖家类型、政策和时间变化。我们如果把一个未经核实的 6% 当成通用费率，SEO 页面即使有流量，也可能因为计算错误把用户赶走。

所以 **v0.5 我建议先做“真实计算引擎”**：

```text
SellerCalc AI v0.5

TikTok Shop Profit Calculator
       ↓
Market
├── US
├── UK
├── Canada
└── Australia

Product Category
       ↓
Platform Fee
       ↓
Affiliate Fee
       ↓
Ads
       ↓
Shipping
       ↓
Refund
       ↓
COGS
       ↓
Real Profit
```

同时加：

**实时/可维护的费率配置文件 + 计算公式说明 + 输入验证 + 结果明细 + Share Result。**

这一步完成以后，我们再开始批量铺 SEO 页面。**先把“算得准”做好，再把“流量”做大。**